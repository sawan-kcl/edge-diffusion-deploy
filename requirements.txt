# Installed INSIDE the NGC PyTorch container (which already provides torch + TensorRT + CUDA).
# Do not pin torch here — use the container's build.

diffusers==0.32.2        # SANA support (SanaPipeline); pinned exact — see note below
transformers==4.46.3     # Gemma-2 text encoder used by SANA; pinned exact — see note below
# diffusers/transformers are pinned to an exact, verified-working pair instead of open
# floors (`>=`). The container's torch build is frozen (nvcr.io/nvidia/pytorch:24.10-py3,
# torch 2.5.0a0), and open-ended pins drifted to newer releases that no longer matched it
# (transformers 5.x needed a torch.distributed.tensor API this torch doesn't have; the
# newest diffusers needed a transformers class this pin doesn't have). This pair is the
# one confirmed to import and run SANA cleanly against this container's torch.
accelerate>=1.0.0        # device placement / cpu offload
sentencepiece            # tokenizer dep for the text encoder
protobuf                 # tokenizer/proto dep
safetensors
Pillow
hf_transfer              # fast parallel downloads (Dockerfile sets HF_HUB_ENABLE_HF_TRANSFER=1)

# metrics / telemetry
torchmetrics>=1.5.0      # CLIPScore (quality proxy); optional at runtime
nvidia-ml-py>=12.560.30  # pynvml — GPU temp / power / VRAM logging

# demo UI (Phase: live demo)
gradio>=4.44.0

# optimization phase (install when you reach Phase C; some are heavy)
# optimum-quanto>=0.2.4  # INT8/FP8 quantization
# torchao                # alternative quantization
